<?php
// Program 1: Vnorena funkce
// Hlavni telo programu
$x = put_string("Zadejte cislo pro vypocet\n");
$a = get_string();
$a = intval($a);

$z = A($a);

function B($a)
{
    $n = 0;
    if($a>5)
    {
       while ($a<5)
       {
            $n=$n+$a;
            $a=$a+1;
       }
    }
    else
    {
        $n = (5*$a)/2;
    }
    //end ifu 
   return  $n;
}         //end funkce

function A($n)
{
  $result = B($n);
  $x=put_string("Vysledek je: ",$result,"\n");
  return $x;
}